<Window x:Class="TrackFilter.Views.AnalysisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TrackFilter.Views" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:TrackFilter.ViewModels"
        mc:Ignorable="d"
        Title="AnalysisWindow" 
        WindowState="Maximized" d:DataContext="{d:DesignInstance viewModels:AnalysisViewModel }">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Open Reference Track"
                          Command="{Binding OpenReferenceCommand}"></MenuItem>
                <MenuItem Header="Open Actual Track"
                          Command="{Binding OpenActualCommand}"></MenuItem>
                <MenuItem Header="Close Window"></MenuItem>
            </MenuItem>
            
        </Menu>
        <TabControl Grid.Row="1">
            <TabItem Header="Map">
                <views:MapControl Tracks="{Binding Tracks}"></views:MapControl>
            </TabItem>
            <TabItem Header="Plot"></TabItem>
        </TabControl>
    </Grid>
</Window>
